/** Shared JPA building blocks (ADR-0015): the one {@code @Embeddable} every entity's amount reuses. */
@NullMarked
package io.github.sudoitir.taraz.adapters.driven.persistence.common;

import org.jspecify.annotations.NullMarked;
