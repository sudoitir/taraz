/**
 * Account aggregate: minimal identity + balance, with its specifications and domain events. Pure Java —
 * no framework dependencies (ADR-0005).
 */
@NullMarked
package io.github.sudoitir.taraz.core.domain.account;

import org.jspecify.annotations.NullMarked;
