/**
 * Domain service seam: {@code PostingService} is the domain's single entry point for
 * credit/debit/transfer/compensate. Pure Java — no framework dependencies (ADR-0005).
 */
@NullMarked
package io.github.sudoitir.taraz.core.domain.service;

import org.jspecify.annotations.NullMarked;
