/**
 * Money value object: BigDecimal minor units, single implicit currency, exact arithmetic with no overflow
 * ceiling (ADR-0036). Pure Java — no framework dependencies (ADR-0005).
 */
@NullMarked
package io.github.sudoitir.taraz.core.domain.money;

import org.jspecify.annotations.NullMarked;
