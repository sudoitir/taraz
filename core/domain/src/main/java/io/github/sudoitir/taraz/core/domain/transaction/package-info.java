/**
 * Transaction aggregate: immutable double-entry legs, type/status/direction enums, leg-shape
 * specifications, compensation (ADR-0035, ADR-0037). Pure Java — no framework dependencies (ADR-0005).
 */
@NullMarked
package io.github.sudoitir.taraz.core.domain.transaction;

import org.jspecify.annotations.NullMarked;
