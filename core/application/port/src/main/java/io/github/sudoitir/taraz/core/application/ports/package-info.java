/**
 * Application ports: inbound (command/query contracts for driving adapters) and outbound (repository/client
 * contracts implemented by driven adapters). Interfaces only — no logic (ADR-0006).
 */
@NullMarked
package io.github.sudoitir.taraz.core.application.ports;

import org.jspecify.annotations.NullMarked;
