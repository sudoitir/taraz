/** Debit use case: command, parsed intent, and handler (ADR-0007 — one package per use case). */
@NullMarked
package io.github.sudoitir.taraz.core.application.service.debit;

import org.jspecify.annotations.NullMarked;
